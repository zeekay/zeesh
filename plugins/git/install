echo -n "I can't find hub would you like me to install it for you? (y/N) "

read ans
if [[ "$ans" == "y" ]]; then
    git clone https://github.com/zeekay/hub $plugin_dir/lib
    echo "...installed hub, $plugin enabled."
fi

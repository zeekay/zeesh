echo -n "I can't find vimpager would you like me to install it for you? (y/N) "

read ans
if [[ "$ans" == "y" ]]; then
    git clone --depth 1 https://github.com/rkitover/vimpager $plugin_dir/lib
    echo "...installed vimpager, $plugin enabled"
fi

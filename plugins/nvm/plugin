### nvm / zeesh! plugin

[ -d $plugin_dir/lib ] || . $plugin_dir/install

export NVM_AUTO_USE=true
export NVM_LAZY_LOAD=true
export NVM_LAZY_LOAD_EXTRA_COMMANDS=('vim')
export NVM_NO_USE=true
source $plugin_dir/lib/zsh-nvm.plugin.zsh;

# vim: ft=zsh

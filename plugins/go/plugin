# zeesh go plugin

# Source GVM if it's installed
[[ -s ~/.gvm/scripts/gvm ]] && source ~/.gvm/scripts/gvm

# Set GOPATH to ~/go
export GOPATH=~/go

# Add Go binaries to path
export PATH=$GOPATH/bin:$PATH

# vim: ft=zsh

# enables zeesh plugins in .zshrc
function zeesh-enable-plugins() {
    for PLUGIN ($zeesh_plugins); do
        PLUGIN_DIR="$HOME/.zsh/plugins/$PLUGIN"
        source "$PLUGIN_DIR/plugin.zsh"
    done
}

function psg() {
    if [ $1 ]; then
        args=($@)
        search=$args[-1]
        args[-1]=()
        ps $args | head -n 1
        ps $args | grep -i --color=always "$search" | grep -v grep
    fi
}
